// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: common.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "hedging.common.v1";

export enum InstrumentClass {
  INSTRUMENT_CLASS_UNSPECIFIED = 0,
  INSTRUMENT_CLASS_EQUITY = 1,
  INSTRUMENT_CLASS_INDEX = 2,
  INSTRUMENT_CLASS_ETF = 3,
  INSTRUMENT_CLASS_OPTION = 4,
  INSTRUMENT_CLASS_FORWARD = 5,
  INSTRUMENT_CLASS_FUTURE = 6,
  INSTRUMENT_CLASS_FX_SPOT = 7,
  INSTRUMENT_CLASS_FX_FORWARD = 8,
  INSTRUMENT_CLASS_BOND = 9,
  UNRECOGNIZED = -1,
}

export function instrumentClassFromJSON(object: any): InstrumentClass {
  switch (object) {
    case 0:
    case "INSTRUMENT_CLASS_UNSPECIFIED":
      return InstrumentClass.INSTRUMENT_CLASS_UNSPECIFIED;
    case 1:
    case "INSTRUMENT_CLASS_EQUITY":
      return InstrumentClass.INSTRUMENT_CLASS_EQUITY;
    case 2:
    case "INSTRUMENT_CLASS_INDEX":
      return InstrumentClass.INSTRUMENT_CLASS_INDEX;
    case 3:
    case "INSTRUMENT_CLASS_ETF":
      return InstrumentClass.INSTRUMENT_CLASS_ETF;
    case 4:
    case "INSTRUMENT_CLASS_OPTION":
      return InstrumentClass.INSTRUMENT_CLASS_OPTION;
    case 5:
    case "INSTRUMENT_CLASS_FORWARD":
      return InstrumentClass.INSTRUMENT_CLASS_FORWARD;
    case 6:
    case "INSTRUMENT_CLASS_FUTURE":
      return InstrumentClass.INSTRUMENT_CLASS_FUTURE;
    case 7:
    case "INSTRUMENT_CLASS_FX_SPOT":
      return InstrumentClass.INSTRUMENT_CLASS_FX_SPOT;
    case 8:
    case "INSTRUMENT_CLASS_FX_FORWARD":
      return InstrumentClass.INSTRUMENT_CLASS_FX_FORWARD;
    case 9:
    case "INSTRUMENT_CLASS_BOND":
      return InstrumentClass.INSTRUMENT_CLASS_BOND;
    case -1:
    case "UNRECOGNIZED":
    default:
      return InstrumentClass.UNRECOGNIZED;
  }
}

export function instrumentClassToJSON(object: InstrumentClass): string {
  switch (object) {
    case InstrumentClass.INSTRUMENT_CLASS_UNSPECIFIED:
      return "INSTRUMENT_CLASS_UNSPECIFIED";
    case InstrumentClass.INSTRUMENT_CLASS_EQUITY:
      return "INSTRUMENT_CLASS_EQUITY";
    case InstrumentClass.INSTRUMENT_CLASS_INDEX:
      return "INSTRUMENT_CLASS_INDEX";
    case InstrumentClass.INSTRUMENT_CLASS_ETF:
      return "INSTRUMENT_CLASS_ETF";
    case InstrumentClass.INSTRUMENT_CLASS_OPTION:
      return "INSTRUMENT_CLASS_OPTION";
    case InstrumentClass.INSTRUMENT_CLASS_FORWARD:
      return "INSTRUMENT_CLASS_FORWARD";
    case InstrumentClass.INSTRUMENT_CLASS_FUTURE:
      return "INSTRUMENT_CLASS_FUTURE";
    case InstrumentClass.INSTRUMENT_CLASS_FX_SPOT:
      return "INSTRUMENT_CLASS_FX_SPOT";
    case InstrumentClass.INSTRUMENT_CLASS_FX_FORWARD:
      return "INSTRUMENT_CLASS_FX_FORWARD";
    case InstrumentClass.INSTRUMENT_CLASS_BOND:
      return "INSTRUMENT_CLASS_BOND";
    case InstrumentClass.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum Side {
  SIDE_UNSPECIFIED = 0,
  SIDE_BUY = 1,
  SIDE_SELL = 2,
  UNRECOGNIZED = -1,
}

export function sideFromJSON(object: any): Side {
  switch (object) {
    case 0:
    case "SIDE_UNSPECIFIED":
      return Side.SIDE_UNSPECIFIED;
    case 1:
    case "SIDE_BUY":
      return Side.SIDE_BUY;
    case 2:
    case "SIDE_SELL":
      return Side.SIDE_SELL;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Side.UNRECOGNIZED;
  }
}

export function sideToJSON(object: Side): string {
  switch (object) {
    case Side.SIDE_UNSPECIFIED:
      return "SIDE_UNSPECIFIED";
    case Side.SIDE_BUY:
      return "SIDE_BUY";
    case Side.SIDE_SELL:
      return "SIDE_SELL";
    case Side.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface Meta {
  /** UUIDv7, producer-generated; consumers dedupe on this */
  msgId: string;
  /** "exo" | "delta-one" | "ui" | "sim" */
  producer: string;
  sentNs: bigint;
  /** bump on additive change; breaking => new message */
  schemaVersion: number;
}

export interface InstrumentRef {
  /** canonical id from refdata/universe.json */
  instrumentId: number;
  /** display only; never key on this */
  symbol: string;
  /** named to avoid Python keyword collision (empirically verified breakage) */
  instrumentClass: InstrumentClass;
  /** ISO 4217 */
  currency: string;
}

export interface ValuationMeta {
  /** Root CLAUDE.md invariant #7: numbers without this block are invalid. */
  modelId: string;
  /** sha256 of canonical param serialization */
  paramsHash: string;
  seed: bigint;
  nPaths: number;
  gitSha: string;
}

function createBaseMeta(): Meta {
  return { msgId: "", producer: "", sentNs: 0n, schemaVersion: 0 };
}

export const Meta: MessageFns<Meta> = {
  encode(message: Meta, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.msgId !== "") {
      writer.uint32(10).string(message.msgId);
    }
    if (message.producer !== "") {
      writer.uint32(18).string(message.producer);
    }
    if (message.sentNs !== 0n) {
      if (BigInt.asUintN(64, message.sentNs) !== message.sentNs) {
        throw new globalThis.Error("value provided for field message.sentNs of type uint64 too large");
      }
      writer.uint32(24).uint64(message.sentNs);
    }
    if (message.schemaVersion !== 0) {
      writer.uint32(32).uint32(message.schemaVersion);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Meta {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMeta();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.msgId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.producer = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.sentNs = reader.uint64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.schemaVersion = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Meta {
    return {
      msgId: isSet(object.msgId)
        ? globalThis.String(object.msgId)
        : isSet(object.msg_id)
        ? globalThis.String(object.msg_id)
        : "",
      producer: isSet(object.producer) ? globalThis.String(object.producer) : "",
      sentNs: isSet(object.sentNs) ? BigInt(object.sentNs) : isSet(object.sent_ns) ? BigInt(object.sent_ns) : 0n,
      schemaVersion: isSet(object.schemaVersion)
        ? globalThis.Number(object.schemaVersion)
        : isSet(object.schema_version)
        ? globalThis.Number(object.schema_version)
        : 0,
    };
  },

  toJSON(message: Meta): unknown {
    const obj: any = {};
    if (message.msgId !== "") {
      obj.msgId = message.msgId;
    }
    if (message.producer !== "") {
      obj.producer = message.producer;
    }
    if (message.sentNs !== 0n) {
      obj.sentNs = message.sentNs.toString();
    }
    if (message.schemaVersion !== 0) {
      obj.schemaVersion = Math.round(message.schemaVersion);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Meta>, I>>(base?: I): Meta {
    return Meta.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Meta>, I>>(object: I): Meta {
    const message = createBaseMeta();
    message.msgId = object.msgId ?? "";
    message.producer = object.producer ?? "";
    message.sentNs = (object.sentNs !== undefined && object.sentNs !== null) ? BigInt(object.sentNs) : 0n;
    message.schemaVersion = object.schemaVersion ?? 0;
    return message;
  },
};

function createBaseInstrumentRef(): InstrumentRef {
  return { instrumentId: 0, symbol: "", instrumentClass: 0, currency: "" };
}

export const InstrumentRef: MessageFns<InstrumentRef> = {
  encode(message: InstrumentRef, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.instrumentId !== 0) {
      writer.uint32(8).uint32(message.instrumentId);
    }
    if (message.symbol !== "") {
      writer.uint32(18).string(message.symbol);
    }
    if (message.instrumentClass !== 0) {
      writer.uint32(24).int32(message.instrumentClass);
    }
    if (message.currency !== "") {
      writer.uint32(34).string(message.currency);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InstrumentRef {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInstrumentRef();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.instrumentId = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.symbol = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.instrumentClass = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.currency = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InstrumentRef {
    return {
      instrumentId: isSet(object.instrumentId)
        ? globalThis.Number(object.instrumentId)
        : isSet(object.instrument_id)
        ? globalThis.Number(object.instrument_id)
        : 0,
      symbol: isSet(object.symbol) ? globalThis.String(object.symbol) : "",
      instrumentClass: isSet(object.instrumentClass)
        ? instrumentClassFromJSON(object.instrumentClass)
        : isSet(object.instrument_class)
        ? instrumentClassFromJSON(object.instrument_class)
        : 0,
      currency: isSet(object.currency) ? globalThis.String(object.currency) : "",
    };
  },

  toJSON(message: InstrumentRef): unknown {
    const obj: any = {};
    if (message.instrumentId !== 0) {
      obj.instrumentId = Math.round(message.instrumentId);
    }
    if (message.symbol !== "") {
      obj.symbol = message.symbol;
    }
    if (message.instrumentClass !== 0) {
      obj.instrumentClass = instrumentClassToJSON(message.instrumentClass);
    }
    if (message.currency !== "") {
      obj.currency = message.currency;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InstrumentRef>, I>>(base?: I): InstrumentRef {
    return InstrumentRef.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InstrumentRef>, I>>(object: I): InstrumentRef {
    const message = createBaseInstrumentRef();
    message.instrumentId = object.instrumentId ?? 0;
    message.symbol = object.symbol ?? "";
    message.instrumentClass = object.instrumentClass ?? 0;
    message.currency = object.currency ?? "";
    return message;
  },
};

function createBaseValuationMeta(): ValuationMeta {
  return { modelId: "", paramsHash: "", seed: 0n, nPaths: 0, gitSha: "" };
}

export const ValuationMeta: MessageFns<ValuationMeta> = {
  encode(message: ValuationMeta, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.modelId !== "") {
      writer.uint32(10).string(message.modelId);
    }
    if (message.paramsHash !== "") {
      writer.uint32(18).string(message.paramsHash);
    }
    if (message.seed !== 0n) {
      if (BigInt.asUintN(64, message.seed) !== message.seed) {
        throw new globalThis.Error("value provided for field message.seed of type uint64 too large");
      }
      writer.uint32(24).uint64(message.seed);
    }
    if (message.nPaths !== 0) {
      writer.uint32(32).uint32(message.nPaths);
    }
    if (message.gitSha !== "") {
      writer.uint32(42).string(message.gitSha);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ValuationMeta {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseValuationMeta();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.modelId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.paramsHash = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.seed = reader.uint64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.nPaths = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.gitSha = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ValuationMeta {
    return {
      modelId: isSet(object.modelId)
        ? globalThis.String(object.modelId)
        : isSet(object.model_id)
        ? globalThis.String(object.model_id)
        : "",
      paramsHash: isSet(object.paramsHash)
        ? globalThis.String(object.paramsHash)
        : isSet(object.params_hash)
        ? globalThis.String(object.params_hash)
        : "",
      seed: isSet(object.seed) ? BigInt(object.seed) : 0n,
      nPaths: isSet(object.nPaths)
        ? globalThis.Number(object.nPaths)
        : isSet(object.n_paths)
        ? globalThis.Number(object.n_paths)
        : 0,
      gitSha: isSet(object.gitSha)
        ? globalThis.String(object.gitSha)
        : isSet(object.git_sha)
        ? globalThis.String(object.git_sha)
        : "",
    };
  },

  toJSON(message: ValuationMeta): unknown {
    const obj: any = {};
    if (message.modelId !== "") {
      obj.modelId = message.modelId;
    }
    if (message.paramsHash !== "") {
      obj.paramsHash = message.paramsHash;
    }
    if (message.seed !== 0n) {
      obj.seed = message.seed.toString();
    }
    if (message.nPaths !== 0) {
      obj.nPaths = Math.round(message.nPaths);
    }
    if (message.gitSha !== "") {
      obj.gitSha = message.gitSha;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ValuationMeta>, I>>(base?: I): ValuationMeta {
    return ValuationMeta.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ValuationMeta>, I>>(object: I): ValuationMeta {
    const message = createBaseValuationMeta();
    message.modelId = object.modelId ?? "";
    message.paramsHash = object.paramsHash ?? "";
    message.seed = (object.seed !== undefined && object.seed !== null) ? BigInt(object.seed) : 0n;
    message.nPaths = object.nPaths ?? 0;
    message.gitSha = object.gitSha ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
