/** Scaffold shell. Screens are built per ui/CLAUDE.md ("Screens" order).
 *  P3.M1 starts with the desk risk board fed by nats.ws subscriptions. */
export function App() {
  return <main>Desk Hedging Platform — UI scaffold (P3.M1 pending, see ui/CLAUDE.md)</main>;
}
