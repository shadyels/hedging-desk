"""Numerical studies that produce a documented artifact under `docs/studies/`, not a runtime
component. Lives under `src/exo/` (not top-level `exo/studies/`) so `mypy --strict src` actually
type-checks it -- see the P2.M1 slice 1 spec.
"""
